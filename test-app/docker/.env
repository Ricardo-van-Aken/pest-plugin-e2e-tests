# ------------------------------------------------------------------
# Autogenerated by docker/scripts/build_compose_env.sh
# Combination: mysql-array-sync-array
# ------------------------------------------------------------------
ENV_FILE=.env.mysql-array-sync-array

# Base settings shared by all stack combinations
IMAGE_REPO=local/e2e-tests
APP_DOMAIN=nginx

# Database preset: MySQL + testing DB
DB_DATABASE=laravel_app_db
DB_USERNAME=admin
DB_PASSWORD=secret
DB_TEST_DATABASE=laravel_app_db_testing
DB_TEST_USERNAME=testing-admin
DB_TEST_PASSWORD=secretttt
DB_ROOT_PASSWORD=verysecret

# Cache preset: Array (no external service)

# Queue preset: sync (in-process)

# Session preset: array (in-memory per worker)

